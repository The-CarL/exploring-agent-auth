# exploring-agent-auth
Exploring the identity and authorization landscape for AI agents calling tools — eight patterns from shared credentials to scoped token exchange, each in a runnable Jupyter notebook. Keycloak, OPA, and FastAPI, all local via Docker Compose.
